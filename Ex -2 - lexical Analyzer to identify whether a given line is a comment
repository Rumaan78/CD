#include <stdio.h>
int main() {
    char str[100];
    printf("Enter comment: ");
    gets(str);
    if(str[0]=='/' && (str[1]=='/' || str[1]=='*'))
        printf("It is a comment\n");
    else
        printf("It is not a comment\n");
    return 0;
}
